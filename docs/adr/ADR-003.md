# ADR-003: PostgreSQL without Redis or Kafka in V1

Status: accepted

## Context

Transactions, interval storage, leases, and queue work fit the same database.

## Decision

Use PostgreSQL, indexed polling, and one backend instance.

## Consequences

WebSocket subscriptions remain in process. Horizontal scaling requires a deliberate broadcast transport change.
