# ADR-002: Modular monolith instead of microservices

Status: accepted

## Context

The initial community is small and consistency spans multiple domains.

## Decision

Use one Java application with feature packages and explicit transactional service operations.

## Consequences

One backend deployment. Domain boundaries support later extraction only if operations demand it.
